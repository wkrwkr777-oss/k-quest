import { ImageResponse } from 'next/og';

export const runtime = 'edge';

export const alt = 'K-Quest - Premium Local Concierge';
export const size = {
    width: 1200,
    height: 630,
};

export const contentType = 'image/png';

export default async function Image() {
    // Inter 폰트 로드 (깔끔하고 읽기 쉬움)
    const fontData = await fetch(
        new URL('https://fonts.gstatic.com/s/inter/v12/UcCO3FwrK3iLTeHuS_fvQtMwCp50KnMw2boKoduKmMEVuI6fAZ9hiA.woff2', import.meta.url)
    ).then((res) => res.arrayBuffer());

    return new ImageResponse(
        (
            <div
                style={{
                    background: 'linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 50%, #0f0f1e 100%)',
                    width: '100%',
                    height: '100%',
                    display: 'flex',
                    flexDirection: 'column',
                    alignItems: 'center',
                    justifyContent: 'center',
                    fontFamily: 'Inter',
                    position: 'relative',
                    overflow: 'hidden',
                }}
            >
                {/* 홀로그램 그리드 배경 */}
                <div
                    style={{
                        position: 'absolute',
                        width: '100%',
                        height: '100%',
                        backgroundImage: 'linear-gradient(rgba(212, 175, 55, 0.03) 1px, transparent 1px), linear-gradient(90deg, rgba(212, 175, 55, 0.03) 1px, transparent 1px)',
                        backgroundSize: '50px 50px',
                        transform: 'perspective(500px) rotateX(60deg)',
                        transformOrigin: 'center center',
                        opacity: 0.2,
                    }}
                />

                {/* 부드러운 글로우 원 */}
                <div
                    style={{
                        position: 'absolute',
                        width: '500px',
                        height: '500px',
                        borderRadius: '50%',
                        background: 'radial-gradient(circle, rgba(212, 175, 55, 0.08) 0%, transparent 70%)',
                        left: '50%',
                        top: '50%',
                        transform: 'translate(-50%, -50%)',
                        filter: 'blur(100px)',
                    }}
                />

                {/* 메인 콘텐츠 */}
                <div
                    style={{
                        display: 'flex',
                        flexDirection: 'column',
                        alignItems: 'center',
                        zIndex: 1,
                    }}
                >
                    {/* K-QUEST 로고 - 심플하고 우아하게 */}
                    <div
                        style={{
                            fontSize: 120,
                            fontWeight: 700,
                            letterSpacing: '-0.02em',
                            background: 'linear-gradient(to bottom, #FFD700, #D4AF37)',
                            backgroundClip: 'text',
                            color: 'transparent',
                            marginBottom: 24,
                            fontFamily: 'Inter',
                        }}
                    >
                        K-QUEST
                    </div>

                    {/* 서브 타이틀 - 깔끔하게 */}
                    <div
                        style={{
                            fontSize: 32,
                            color: '#D4AF37',
                            marginBottom: 32,
                            fontWeight: 600,
                            letterSpacing: '0.15em',
                            textTransform: 'uppercase',
                            fontFamily: 'Inter',
                        }}
                    >
                        Premium Local Concierge
                    </div>

                    {/* 설명 - 직관적으로 */}
                    <div
                        style={{
                            fontSize: 24,
                            color: 'rgba(255, 255, 255, 0.7)',
                            maxWidth: 700,
                            textAlign: 'center',
                            lineHeight: 1.5,
                            fontFamily: 'Inter',
                            marginBottom: 48,
                        }}
                    >
                        Connect with verified local experts for VIP experiences in Korea
                    </div>

                    {/* 태그들 - 단순하고 명확하게 */}
                    <div
                        style={{
                            display: 'flex',
                            gap: 16,
                        }}
                    >
                        {['VIP Travel', 'Dining', 'Shopping', 'Business'].map((tag) => (
                            <div
                                key={tag}
                                style={{
                                    padding: '10px 24px',
                                    background: 'rgba(212, 175, 55, 0.1)',
                                    border: '1px solid rgba(212, 175, 55, 0.3)',
                                    borderRadius: 8,
                                    color: '#D4AF37',
                                    fontSize: 18,
                                    fontFamily: 'Inter',
                                    fontWeight: 500,
                                }}
                            >
                                {tag}
                            </div>
                        ))}
                    </div>
                </div>

                {/* 하단 라인 - 심플하게 */}
                <div
                    style={{
                        position: 'absolute',
                        bottom: 0,
                        left: 0,
                        right: 0,
                        height: '2px',
                        background: 'linear-gradient(90deg, transparent, #D4AF37 50%, transparent)',
                        opacity: 0.5,
                    }}
                />
            </div>
        ),
        {
            ...size,
            fonts: [
                {
                    name: 'Inter',
                    data: fontData,
                    style: 'normal',
                    weight: 700,
                },
            ],
        }
    );
}
